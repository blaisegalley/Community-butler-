# Copy to .env.local for local dev. Base URL of the marketing server (see
# /server) — only needed for the admin Marketing tab (caption generation,
# publishing, seasonal drafts). The rest of the site works without it.
VITE_MARKETING_API_BASE=http://localhost:8787
