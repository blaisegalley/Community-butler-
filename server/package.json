{
  "name": "community-butler-marketing-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Backend for the Marketing Agent feature. Holds the Anthropic and Meta Graph API secrets server-side — see README.md for why this exists and what it needs to run.",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
